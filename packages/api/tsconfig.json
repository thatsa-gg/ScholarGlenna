{
    "extends": "../../common/config/tsconfig.base.json",
    "compilerOptions": {
        "rootDir": "./src",
        "outDir": "./dist",
        "lib": [
            "ES2022",
            "DOM" // hack so TSC doesn't complain about fetch
        ]
    }
}
