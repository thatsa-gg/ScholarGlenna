<style lang="scss">
    a {
        grid-area: title;
        z-index: var(--level-title);
        background: var(--secondary-background);
        box-shadow: var(--shadow-border);
        color: var(--text);
        display: block;
        clip-path: inset(0 0 -6px 0)
    }
</style>
<a href="/-/dashboard">thatsa/gg</a>
