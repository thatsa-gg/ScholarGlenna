{
    "name": "@glenna/prisma",
    "version": "0.0.1",
    "type": "module",
    "private": true,
    "main": "dist/index.js",
    "types": "src/index.ts",
    "devDependencies": {
        "@rushstack/heft": "^0.50.0",
        "typescript": "^5.0.3",
        "prisma": "^4.12.0",
        "@types/node": "^18"
    },
    "dependencies": {
        "@prisma/client": "^4.12.0",
        "@js-temporal/polyfill": "^0.4.3",
        "zod": "^3.21.4",
        "@glenna/discord": "workspace:*",
        "@glenna/util": "workspace:*"
    },
    "scripts": {
        "migrate": "prisma migrate deploy",
        "migrate-reset": "prisma migrate reset --skip-generate",
        "migrate-status": "prisma migrate status",
        "generate": "prisma generate",
        "build": "heft build --clean",
        "clean": "heft clean && prisma generate"
    },
    "prisma": {
        "schema": "src/schema.prisma"
    }
}
